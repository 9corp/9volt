language: go
sudo: false
go:
  - 1.6.2
  - tip
env:
  global:
    - GOARCH=amd64

before_install:
 - go get github.com/my_project_path
 - go get github.com/tools/godep

install:
 - godep restore

script:
  - go test -v ./...